
//Create a variable named day and Assign it a the current day of the week as string. 
const day = 'Friday'
//Create another variable name position and assign it a number which represents the
//position of the current day in days of the week

const position = 5
// Create another variable named output and assign it a string that reads:
// Today is Thursday. It is the 4th day of the week. (Use string concatenation to add the
// variables declared in steps and 2 above)

const output = 'Today is '+ day + '. it is the '+ position +'th ' + 'day of the week.'

console.log(output)